export class User {
    access_token: string;
    token_type: string;
    userName: string;
    userId: string;
}

export class UserRegister {
    email: string;
    password: string;
    confirmPassword: string;
}